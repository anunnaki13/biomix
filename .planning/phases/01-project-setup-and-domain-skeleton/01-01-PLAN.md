---
phase: 01-project-setup-and-domain-skeleton
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - next.config.ts
  - tsconfig.json
  - tailwind.config.ts
  - postcss.config.js
  - components.json
  - vitest.config.ts
  - src/app/globals.css
autonomous: true
requirements:
  - SETUP-01
  - SETUP-03
user_setup: []
must_haves:
  truths:
    - The BIOMIX repo runs as a Next.js TypeScript project with required UI, form, state, validation, chart, and test dependencies installed.
    - The project contains the baseline config files needed for development and testing.
  artifacts:
    - package.json
    - tsconfig.json
    - next.config.ts
    - tailwind.config.ts
    - postcss.config.js
    - vitest.config.ts
  key_links:
    - package scripts call the installed Next.js and Vitest toolchain.
    - Tailwind and global CSS are wired into the app scaffold.
---

<objective>
Scaffold the BIOMIX project runtime and baseline tooling.

Purpose: Establish the runnable framework and toolchain so the rest of Phase 1 has a stable base.
Output: Project config files, installed dependencies, and baseline app styling/tooling integration.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BIOMIX_Feasibility_Engine_Blueprint.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Initialize Next.js TypeScript app dependencies and config</name>
  <files>package.json, next.config.ts, tsconfig.json, postcss.config.js, tailwind.config.ts</files>
  <read_first>BIOMIX_Feasibility_Engine_Blueprint.md, .planning/phases/01-project-setup-and-domain-skeleton/01-CONTEXT.md</read_first>
  <action>Create the Next.js App Router project in the current repo without nesting a second repo. Install the core stack required by the blueprint: Next.js, React, TypeScript, Tailwind CSS, shadcn/ui prerequisites, Recharts, Lucide React, React Hook Form, Zod, Zustand, and Vitest. Define scripts for `dev`, `build`, `lint`, and `test` in `package.json`.</action>
  <verify>`npm run build`</verify>
  <acceptance_criteria>
    - `package.json` includes Next.js app scripts and BIOMIX dependencies.
    - `tsconfig.json`, `next.config.ts`, `postcss.config.js`, and `tailwind.config.ts` exist.
  </acceptance_criteria>
  <done>Framework and tooling files exist and build successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Wire baseline styling and test configuration</name>
  <files>src/app/globals.css, vitest.config.ts, components.json</files>
  <read_first>BIOMIX_Feasibility_Engine_Blueprint.md</read_first>
  <action>Set up Tailwind global styles with BIOMIX-aligned CSS variables as a starting point, add Vitest configuration for future calculation tests, and add any shadcn/ui config file needed for component generation. Keep the styling scaffold lightweight but domain-specific, not a default starter theme.</action>
  <verify>`npm run test -- --runInBand` or the repo's equivalent single-run vitest command</verify>
  <acceptance_criteria>
    - `src/app/globals.css` defines BIOMIX baseline variables or tokens.
    - `vitest.config.ts` exists and can load a simple test suite later.
    - Styling setup does not block Next.js build.
  </acceptance_criteria>
  <done>Styling and test scaffolding are wired and ready for downstream code.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm install` completed successfully
- [ ] `npm run build` passes
- [ ] `npm run test` executes without configuration failure
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- BIOMIX has a stable framework and tooling base for the rest of Phase 1
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-and-domain-skeleton/01-01-SUMMARY.md`
</output>
