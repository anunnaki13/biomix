---
phase: 01-project-setup-and-domain-skeleton
plan: 03
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - src/types/biomass.ts
  - src/types/results.ts
  - src/lib/defaults/scenarios.ts
  - src/lib/validators/scenarioSchema.ts
  - src/lib/formatters/currency.ts
  - src/lib/formatters/number.ts
  - src/lib/formatters/percentage.ts
  - src/store/scenarioStore.ts
autonomous: true
requirements:
  - DATA-01
  - DATA-02
  - DATA-03
  - DATA-04
  - DATA-05
  - DATA-06
user_setup: []
must_haves:
  truths:
    - BIOMIX has typed scenario and result contracts aligned with the blueprint.
    - The default 20 TPD scenario and scenario preset data exist.
    - Zod validation and Indonesian formatting helpers exist for later forms and dashboard work.
  artifacts:
    - src/types/biomass.ts
    - src/types/results.ts
    - src/lib/defaults/scenarios.ts
    - src/lib/validators/scenarioSchema.ts
    - src/lib/formatters/currency.ts
    - src/lib/formatters/number.ts
    - src/lib/formatters/percentage.ts
    - src/store/scenarioStore.ts
  key_links:
    - Scenario defaults match the declared BIOMIX types.
    - Validation rules enforce the blueprint’s MVP constraints.
---

<objective>
Define BIOMIX domain contracts, defaults, validation, formatting, and baseline state.

Purpose: Give later phases a stable and typed business surface before formula and UI depth grows.
Output: Type files, default scenario exports, scenario schema, formatters, and a baseline scenario store.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@BIOMIX_Feasibility_Engine_Blueprint.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement BIOMIX input and result type contracts</name>
  <files>src/types/biomass.ts, src/types/results.ts</files>
  <read_first>BIOMIX_Feasibility_Engine_Blueprint.md</read_first>
  <action>Translate the blueprint TypeScript data model and result contracts into project source files. Preserve the BIOMIX domain naming so later calculation files can plug in directly without renaming types.</action>
  <verify>`rg -n \"export interface|export type\" src/types`</verify>
  <acceptance_criteria>
    - `src/types/biomass.ts` defines scenario input contracts.
    - `src/types/results.ts` defines feasibility result contracts.
  </acceptance_criteria>
  <done>The repo has stable BIOMIX type contracts for inputs and outputs.</done>
</task>

<task type="auto">
  <name>Task 2: Add default scenarios, validators, formatters, and baseline store</name>
  <files>src/lib/defaults/scenarios.ts, src/lib/validators/scenarioSchema.ts, src/lib/formatters/currency.ts, src/lib/formatters/number.ts, src/lib/formatters/percentage.ts, src/store/scenarioStore.ts</files>
  <read_first>BIOMIX_Feasibility_Engine_Blueprint.md</read_first>
  <action>Implement the default 20 TPD BIOMIX scenario plus conservative/base/optimistic preset support, add Zod schema rules from the blueprint, create Indonesian formatting helpers, and scaffold a Zustand store that can hold the active scenario even if persistence comes later.</action>
  <verify>`npm run build`</verify>
  <acceptance_criteria>
    - Default scenario exports include the 20 TPD mixed biomass case.
    - Validation rules cover mix total, pricing mode requirements, financing constraints, and operating limits.
    - Currency/number/percentage formatters target Indonesian display.
  </acceptance_criteria>
  <done>Defaults, validation, formatting, and baseline state are ready for dashboard and forms.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run build` passes
- [ ] Default scenario data compiles against BIOMIX types
- [ ] Validator and formatter modules are importable without runtime errors
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- BIOMIX has stable typed defaults and validation for downstream phases
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-and-domain-skeleton/01-03-SUMMARY.md`
</output>
