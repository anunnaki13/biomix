---
phase: 01-project-setup-and-domain-skeleton
plan: 04
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
  - "01-03"
files_modified:
  - README.md
  - src/tests/smoke.test.ts
autonomous: true
requirements:
  - SETUP-04
user_setup: []
must_haves:
  truths:
    - The repo documents what BIOMIX is, what stack it uses, and how to develop it.
    - The repo includes at least one smoke-level test or verification anchor for the scaffold.
    - The developer run workflow includes checking active ports before starting the app on the VPS.
  artifacts:
    - README.md
    - src/tests/smoke.test.ts
  key_links:
    - README development commands match the actual project scripts.
    - The verification path references the port-check rule before running the dev server.
---

<objective>
Document the BIOMIX scaffold and verify the Phase 1 foundation cleanly.

Purpose: Make the repo understandable for future sessions and ensure the new project baseline is reproducible.
Output: README, smoke verification coverage, and a documented safe dev-server startup flow.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-setup-and-domain-skeleton/01-01-PLAN.md
@.planning/phases/01-project-setup-and-domain-skeleton/01-02-PLAN.md
@.planning/phases/01-project-setup-and-domain-skeleton/01-03-PLAN.md
@BIOMIX_Feasibility_Engine_Blueprint.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write BIOMIX README from the implemented scaffold</name>
  <files>README.md</files>
  <read_first>BIOMIX_Feasibility_Engine_Blueprint.md, .planning/PROJECT.md</read_first>
  <action>Create a concise README that explains the BIOMIX purpose, key MVP features, tech stack, and actual development commands. Include a note that formulas are built in later phases and that port usage must be checked before starting the dev server on the VPS.</action>
  <verify>`rg -n \"BIOMIX Feasibility Engine|npm run dev|npm run test\" README.md`</verify>
  <acceptance_criteria>
    - README identifies BIOMIX as a biomass pellet feasibility app.
    - README lists the real stack and dev commands.
    - README mentions safe port checking before running the server.
  </acceptance_criteria>
  <done>The repo has an accurate onboarding document for the scaffolded project.</done>
</task>

<task type="auto">
  <name>Task 2: Add smoke verification for the Phase 1 scaffold</name>
  <files>src/tests/smoke.test.ts</files>
  <read_first>src/types/biomass.ts, src/lib/defaults/scenarios.ts</read_first>
  <action>Add a small smoke test that proves the default scenario data and core scaffold imports load correctly. Keep it lightweight and focused on the Phase 1 foundation rather than future formula outputs.</action>
  <verify>`npm run test`</verify>
  <acceptance_criteria>
    - The test suite includes at least one BIOMIX scaffold smoke test.
    - The smoke test exercises real project exports, not a dummy placeholder.
  </acceptance_criteria>
  <done>The Phase 1 scaffold has a repeatable verification anchor.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test` passes
- [ ] `npm run build` passes
- [ ] README matches real commands and mentions port checking before dev server startup
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- BIOMIX scaffold is documented and reproducibly verifiable
</success_criteria>

<output>
After completion, create `.planning/phases/01-project-setup-and-domain-skeleton/01-04-SUMMARY.md`
</output>
